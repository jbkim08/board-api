<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.board.mapper.CommentMapper">

    <insert id="insert" parameterType="Comment" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO comment (board_id, content, writer, created_at, updated_at)
        VALUES (#{boardId}, #{content}, #{writer}, CURRENT_TIMESTAMP, CURRENT_TIMESTAMP)
    </insert>

    <select id="findById" resultType="Comment">
        SELECT id, board_id, content, writer, created_at, updated_at
        FROM comment
        WHERE id = #{id}
    </select>

    <select id="findByBoardId" resultType="Comment">
        SELECT id, board_id, content, writer, created_at, updated_at
        FROM comment
        WHERE board_id = #{boardId}
        ORDER BY id ASC
    </select>

    <delete id="deleteById">
        DELETE FROM comment
        WHERE id = #{id}
    </delete>

</mapper>